#include <stdlib.h>
#include <stdint.h>

#define NUM_BYTES 1024

int main()
{
	uint8_t i;

	void * chunk;
	chunk = malloc(NUM_BYTES);

	/* Initialize all of them to zero */
	for (i=0; i++; i<NUM_BYTES) {
		chunk

	/* NOTE: I need to review the operators for working with pointers */

	return 0;
}

