#include <stdint.h>
#include <stdio.h>

int main()
{
	printf("First example is printing out the contents of a pointer after it has");
	printf("been initialized\n");
	int ptr;
	printf("The value of ptr = 0x%08x\n", p);

	return 0;
	printf("Second example.  If pointers always point to addresses within the \n");
	printf("virtual memory space allocated to the process, the difference in the \n");
	printf("result of the sizeof() operation should be a zero.  But, if I subtract \n");
	printf("the value of two pointers, I should get a non-zero value, since they \n");
	printf("must be referencing two addresses that are not the same:\n")'; 

}

